{
  "name": "pigletparkpounder",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/server/index.ts",
    "test": "bun test",
    "build:client": "bun build ./src/client/main.ts --outfile public/app.js --minify",
    "build": "bun run build:client && bun run scripts/build.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "fflate": "^0.8.3"
  }
}
